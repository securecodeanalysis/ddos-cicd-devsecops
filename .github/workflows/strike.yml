name: Run Docker Command

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  run-docker-command:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Docker command
        uses: addnab/docker-run-action@v3
        with:
            image: cyberhack255/strike:latest
            run: | 
                  attack https://testngwaf.global.ssl.fastly.net 300 1000 $PROXY 100
  

